# ملخص سريع - كيف تبدأ؟

## ❌ بخصوص Gemini Nano

**Gemini Nano لا يدعم توليد الصور!** 
- Gemini Nano من Google للنصوص والمحادثة فقط
- لتوليد الصور، استخدم أحد الخيارات التالية

---

## ✅ الحلول المتاحة (3 خيارات)

### 1. Replicate ⭐ (موصى به - ابدأ من هنا)

**لماذا؟**
- ✅ سهل جداً
- ✅ رخيص ($0.002 للصورة)
- ✅ رصيد مجاني للبدء
- ✅ لا يحتاج بطاقة ائتمان

**كيف؟**
1. اذهب: https://replicate.com
2. سجل حساب (مجاني)
3. خذ API Token من: https://replicate.com/account/api-tokens
4. ثبت المكتبة: `pip install replicate`
5. افتح `app.py` → ابحث عن `# الخيار 1: Replicate` → أزل التعليق (#)

**التكلفة:** ~$0.002 للصورة (رخيص جداً)

---

### 2. OpenAI DALL-E

**لماذا؟**
- ✅ جودة عالية جداً
- ✅ سريع

**لماذا لا؟**
- ❌ غالي ($0.04 للصورة)
- ❌ يحتاج بطاقة ائتمان

**كيف؟**
1. اذهب: https://platform.openai.com
2. سجل حساب (يحتاج بطاقة)
3. خذ API Key من: https://platform.openai.com/api-keys
4. ثبت المكتبة: `pip install openai`
5. افتح `app.py` → ابحث عن `# الخيار 2: OpenAI DALL-E` → أزل التعليق (#)

**التكلفة:** ~$0.04 للصورة

---

### 3. Hugging Face (مجاني)

**لماذا؟**
- ✅ مجاني
- ✅ لا يحتاج بطاقة

**لماذا لا؟**
- ❌ أبطأ
- ❌ محدود في الاستخدام المجاني

**كيف؟**
1. اذهب: https://huggingface.co
2. سجل حساب (مجاني)
3. خذ Token من: https://huggingface.co/settings/tokens
4. ثبت المكتبات: `pip install huggingface_hub requests Pillow`
5. افتح `app.py` → ابحث عن `# الخيار 3: Hugging Face` → أزل التعليق (#)

**التكلفة:** مجاني (محدود)

---

## 📋 خطوات سريعة (5 دقائق)

### 1. ثبت المكتبات الأساسية
```bash
pip install -r requirements.txt
```

### 2. ثبت مكتبة API (اختر واحدة)
```bash
# لل Replicate
pip install replicate

# أو لل OpenAI
pip install openai

# أو لل Hugging Face
pip install huggingface_hub requests Pillow
```

### 3. أنشئ ملف `.env`
انسخ `env.example` إلى `.env` واملأ البيانات:
```env
DB_HOST=localhost
DB_NAME=ramadan_app
DB_USER=root
DB_PASSWORD=

# أضف API Key هنا (اختر واحد فقط)
REPLICATE_API_TOKEN=your_token_here
```

### 4. شغّل قاعدة البيانات
شغّل `schema.sql` في MySQL/phpMyAdmin

### 5. فعّل الكود في `app.py`
- افتح `app.py`
- ابحث عن الخيار الذي اخترته (Replicate/OpenAI/HF)
- أزل التعليق (#) من الكود

### 6. شغّل التطبيق
```bash
python app.py
```

---

## 📚 ملفات المساعدة

- `QUICK_START_AR.md` - دليل تفصيلي خطوة بخطوة
- `API_GUIDE_AR.md` - شرح مفصل لكل API
- `README.md` - الوثائق الكاملة

---

## 💡 نصيحة

**ابدأ بـ Replicate** - الأسهل والأرخص والأسرع للبدء!

---

## ❓ مشاكل شائعة

**"API Key not found"**
→ تأكد من وجود ملف `.env` واسم المتغير صحيح

**"Module not found"**
→ ثبت المكتبة: `pip install replicate` (أو openai أو huggingface_hub)

**"Database connection error"**
→ تأكد من تشغيل MySQL/XAMPP

---

## 🎉 جاهز!

بعد اتباع الخطوات، التطبيق سيعمل ويمكنك توليد الصور!
