# دليل البدء السريع - خطوة بخطوة

## الخطوة 1: تثبيت المكتبات الأساسية

```bash
pip install -r requirements.txt
```

## الخطوة 2: إعداد قاعدة البيانات

1. افتح phpMyAdmin أو MySQL
2. شغّل ملف `schema.sql` لإنشاء قاعدة البيانات

أو من سطر الأوامر:
```bash
mysql -u root -p < schema.sql
```

## الخطوة 3: إعداد ملف البيئة (.env)

أنشئ ملف `.env` في مجلد المشروع:

```env
# Database
DB_HOST=localhost
DB_NAME=ramadan_app
DB_USER=root
DB_PASSWORD=

# اختر واحد فقط من الخيارات التالية:

# لل Replicate (موصى به)
REPLICATE_API_TOKEN=your_token_here

# أو لل OpenAI DALL-E
# OPENAI_API_KEY=sk-your_key_here

# أو لل Hugging Face
# HUGGINGFACE_API_KEY=your_token_here
```

## الخطوة 4: الحصول على API Key

### خيار A: Replicate (الأسهل) ⭐

1. اذهب إلى: https://replicate.com
2. سجل حساب (Sign Up) - مجاني
3. اذهب إلى: https://replicate.com/account/api-tokens
4. انسخ الـ Token
5. ضعه في `.env` كـ `REPLICATE_API_TOKEN`
6. ثبت المكتبة:
   ```bash
   pip install replicate
   ```
7. افتح `app.py` وابحث عن `# الخيار 1: Replicate`
8. أزل التعليق (#) من الكود الموجود هناك

### خيار B: OpenAI DALL-E

1. اذهب إلى: https://platform.openai.com
2. سجل حساب (يحتاج بطاقة ائتمان)
3. اذهب إلى: https://platform.openai.com/api-keys
4. أنشئ API Key جديد
5. ضعه في `.env` كـ `OPENAI_API_KEY`
6. ثبت المكتبة:
   ```bash
   pip install openai
   ```
7. افتح `app.py` وابحث عن `# الخيار 2: OpenAI DALL-E`
8. أزل التعليق (#) من الكود الموجود هناك

### خيار C: Hugging Face (مجاني)

1. اذهب إلى: https://huggingface.co
2. سجل حساب مجاني
3. اذهب إلى: https://huggingface.co/settings/tokens
4. أنشئ Token جديد
5. ضعه في `.env` كـ `HUGGINGFACE_API_KEY`
6. ثبت المكتبات:
   ```bash
   pip install huggingface_hub requests Pillow
   ```
7. افتح `app.py` وابحث عن `# الخيار 3: Hugging Face`
8. أزل التعليق (#) من الكود الموجود هناك

## الخطوة 5: إضافة صورة الشخصية الأصلية

ضع صورة الشخصية الكارتونية في:
```
static/images/original-character.jpg
```

## الخطوة 6: تشغيل التطبيق

```bash
python app.py
```

افتح المتصفح على: **http://localhost:5000**

## ملاحظات مهمة:

- ⚠️ **Gemini Nano لا يدعم توليد الصور** - استخدم أحد الخيارات أعلاه
- 💡 **ابدأ بـ Replicate** - الأسهل والأرخص
- 🔒 **لا تشارك ملف `.env`** - يحتوي على مفاتيح سرية
- 📝 تأكد من إزالة التعليق (#) من الكود في `app.py` بعد اختيار الخدمة

## حل المشاكل:

### خطأ: "API Key not found"
- تأكد من وجود ملف `.env` في نفس مجلد `app.py`
- تأكد من كتابة اسم المتغير بشكل صحيح (حساس لحالة الأحرف)

### خطأ: "Module not found"
- ثبت المكتبة المطلوبة: `pip install replicate` أو `pip install openai`

### خطأ في الاتصال بقاعدة البيانات
- تأكد من تشغيل MySQL/XAMPP
- تحقق من بيانات الاتصال في ملف `.env`
